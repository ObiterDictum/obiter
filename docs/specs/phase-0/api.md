# Phase 0 API

## Auth

`/api/auth/*`

## Matters

`POST /api/matters`

`GET /api/matters`

`GET /api/matters/:matterId`

## Documents

`POST /api/matters/:matterId/documents`

`GET /api/matters/:matterId/documents`

`GET /api/documents/:documentId`

## Reports

`GET /api/matters/:matterId/reports`
