# UK Legal PII benchmark

Publication is blocked pending the mandatory provider terms review and maintainer dry-run blind review. `MANIFEST.json` is intentionally empty until that gate is passed; no benchmark document is published by this branch.

After approval, this directory will contain `documents.jsonl`, the content-hash manifest, `LICENSE`, `DATASHEET.md`, generation statistics, and the committed Haiku QA report. The dataset will contain fictional UK legal documents only; it must never contain real personal data.
