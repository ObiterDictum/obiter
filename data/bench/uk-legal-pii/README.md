# UK Legal PII benchmark

Publication is blocked pending the mandatory provider terms review, adjudication, and maintainer approval. `MANIFEST.json` is intentionally empty; no benchmark document is published by this branch.

The frozen benchmark corpus will be released from a dedicated public benchmark repository, not from this product repository. This directory retains only the release contract, including `LICENSE`, `DATASHEET.md`, and manifest/schema placeholders. Do not add `documents.jsonl`, raw generation output, or review evidence here. The dataset will contain fictional UK legal documents only; it must never contain real personal data.
